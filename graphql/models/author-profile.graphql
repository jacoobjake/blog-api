"Author profile for blog attribution."
type AuthorProfile {
    "Unique primary key."
    id: ID!

    "Linked user account, if any."
    user: User @belongsTo(relation: "user")

    "Display name."
    name: String!

    "Short biography."
    bio: String
}
